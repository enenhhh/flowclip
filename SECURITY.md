# 安全政策

## 支持范围

FlowClip 目前只为最新发布版本和 `main` 分支接受安全修复。旧版本发现的问题可能需要先升级到最新版本才能获得修复。

## 私密报告漏洞

请不要在公开 Issue、Discussion、Pull Request、日志或截图中披露尚未修复的漏洞、共享密钥、签名材料或可复现的攻击细节。

请打开本仓库的 **Security** 页面，进入 **Advisories**，选择 **Report a vulnerability**，使用 GitHub Private Vulnerability Reporting 提交报告。报告应尽量包含：

- 受影响的版本、平台和网络拓扑；
- 问题的影响与可复现步骤；
- 最小化的验证代码、请求或日志；
- 已知的缓解措施；
- 是否已经向其他人公开。

维护者会在同一个私密 Advisory 中确认报告、讨论修复和协调披露。项目不承诺固定响应时限；如需补充信息，请继续使用该私密线程。

不包含敏感信息的安全加固建议可以提交普通 Issue。

## 安全边界

FlowClip 的共享密钥用于请求鉴权，不提供传输加密，也不是端到端加密。不要将内置 HTTP 服务直接暴露到互联网；公网使用必须部署 HTTPS 隧道或受控的加密虚拟网络。配置、日志、截图和诊断信息都可能包含服务器地址或其他敏感上下文，提交前请先脱敏。

泄露的共享密钥应立即在所有设备上轮换。Android Release 签名密钥、Apple 签名材料和 Windows 代码签名材料不得提交到仓库或附加到 Issue。
